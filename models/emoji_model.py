import numpy as np
import PIL
import tensorflow
import pathlib

from keras.layers import Flatten, Dense
from keras.models import Sequential
from keras.optimizers import Adam
from keras.applications import ResNet50

class EmojiModel:
    def __init__(self):
        self.img_width, self.img_height = 180, 180
 # TODO all this shit later

    
